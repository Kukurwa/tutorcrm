services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    image: tutorcrm/web:dev
    env_file: .env
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    restart: unless-stopped
